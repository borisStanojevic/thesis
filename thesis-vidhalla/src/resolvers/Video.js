const Video = {
  // uploader(parent, args, {
  //     db
  // }, info) {
  //     return db.users.find(user => {
  //         return user.id === parent.uploader;
  //     });
  // },
  // comments(parent, args, {
  //     db
  // }, info) {
  //     return db.comments.filter(comment => comment.video === parent.id);
  // }
};

export default Video;
